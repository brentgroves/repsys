---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: %APP%-%NODE%-pvc
spec:
  storageClassName: mysql-storageclass
  volumeMode: Filesystem
  resources:
    requests:
      storage: 20Gi

      
